[package]
name = "agent"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
sysinfo = "0.38.4"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"